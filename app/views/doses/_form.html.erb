<div class="container">
  <%= simple_form_for [cocktail, dose] do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient, prompt: "Choose something punk" %>
    <%= f.submit "Save" %>
  <% end %>
</div>
